def GenereteBanch(branch, ban, n):
    if (len(branch) == n):
        print(ban)
        return

    for i in range(len(branch)):
        ban.append(branch[i])
        GenereteBanch(branch, ban, n + 1)
        ban.pop()


if __name__ == "__main__":
    branch = [123, 'Gpt', 567, 'Gpt', 78.54]
    for i in branch:
        if branch.count(i) > 1:
            branch.remove(i)
    ban = []
    GenereteBanch(branch, ban, 0)
