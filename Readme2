#### Лабораторная работа №1

- `Цель` - Добавить элементы в начало или конец дека. Удалить элементы из начала или конца дека.
- `Задача` - реализовать алгоритм дека
- `Вариант` - $\textcolor{grey}{\textit{18}}$

## Список ключевых понятий (определения) ❓

- `Деком (deque)` -  называется структура, в которой добавление и извлечение элементов
возможно с двух сторон. Т.е. это некоторая смесь стека и очереди.

## Алгоритм

- 1. Создайте пустой дек.
- 2. Добавление элемента в начало дека:
 Проверьте, есть ли свободное место в памяти для добавления нового элемента.
 Если есть, добавьте новый элемент в начало дека (сдвинув остальные элементы).
 Если нет свободного места, дек считается переполненным.
- 3. Удаление элемента с начала дека:
Проверьте, содержит ли дек хотя бы один элемент.
Если да, удалите элемент из начала дека (сдвинув остальные элементы).
Если нет элементов, дек считается пустым.
- 4. Добавление элемента в конец дека:
Проверьте, есть ли свободное место в памяти для добавления нового элемента.
Если есть, добавьте новый элемент в конец дека.
Если нет свободного места, дек считается переполненным.
Удаление элемента с конца дека:
Проверьте, содержит ли дек хотя бы один элемент.
Если да, удалите элемент из конца дека.
Если нет элементов, дек считается пустым.
- 5. Проверка размера дека:
Получите текущий размер дека.
Получение первого элемента дека:
Проверьте, содержит ли дек хотя бы один элемент.
Если да, получите значение первого элемента.
Получение последнего элемента дека:
Проверьте, содержит ли дек хотя бы один элемент.
Если да, получите значение последнего элемента.
