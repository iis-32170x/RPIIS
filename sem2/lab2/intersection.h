#pragma once
#include <iostream>
#include <fstream>;
#include <string>
#include <vector>
using namespace std;
string fromFile(string filename);
void spacetest(string first_set, string second_set);
string intersection(string first_set, string second_set);
void set—hecking(string first_set, string second_set);